import { initializeApp } from 'firebase/app';
// This file is a placeholder since Firebase is not yet set up.
// If you decide to use Firebase later, the set_up_firebase tool will update this correctly.
export const db = {};
export const auth = {};
